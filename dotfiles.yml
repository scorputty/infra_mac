---

  - name: Create or sync $HOME/dotfiles
    synchronize:
      src: dotfiles
      dest: "{{ ansible_env.HOME }}"
      delete: yes
      recursive: yes
    tags: syncdotdir

  # - name: Setting up / syncing bash profile
  #
  #
  # - name: Setting up / syncing git profile
  #
  #
  # - name: Setting up / syncing iTerm2 profile
